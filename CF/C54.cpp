/**
*     author:  AlgoHacker3112
*    created: 2022-11-01 16:26:56
**/ 

#include <bits/stdc++.h>
#define all(a) begin(a),end(a)
#ßdefine ll long long

using namespace std;

 #ifdef DEBUG
#include <bits/debug.h> // replace with your path
#else
#define debug(x)
#endif


int32_t main(){
ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

 int t=1; 
 cin>>t;
 while(t--){
 int n; cin >> n;
 ll l = 1e7, r=1e12;
 while(l <= r) {
    ll mid = l + (r-l)/2;
    double
    if((n-))
 }

 }
cerr << "Time elapsed: " << 1.0 * clock() / CLOCKS_PER_SEC << " s."<<endl;

    return 0;
}